# Copy to .env and add any environment variable overrides
# Override the local LLM and embedding model names, if needed
# e.g.
LOCAL_LLM_MODEL=google/flan-t5-small
LOCAL_EMBEDDING_MODEL=google/embeddinggemma-300m
# HUGGINGFACEHUB_API_TOKEN=hf_xxx (optional for larger HF models)
